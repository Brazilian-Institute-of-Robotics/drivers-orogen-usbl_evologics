# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(usbl_evologicsTaskLib)
ADD_LIBRARY(${USBL_EVOLOGICS_TASKLIB_NAME} SHARED 
    ${USBL_EVOLOGICS_TASKLIB_SOURCES})
add_dependencies(${USBL_EVOLOGICS_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${USBL_EVOLOGICS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${USBL_EVOLOGICS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${USBL_EVOLOGICS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${USBL_EVOLOGICS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${USBL_EVOLOGICS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${USBL_EVOLOGICS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${USBL_EVOLOGICS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${USBL_EVOLOGICS_TASKLIB_HEADERS}
    DESTINATION include/orocos/usbl_evologics)

